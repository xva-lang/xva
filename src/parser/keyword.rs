// fn abcd_parser(i: &str) -> IResult<&str, &str> {
//     tag("abcd")(i) // will consume bytes if the input begins with "abcd"
//   }

// use nom::{bytes::complete::tag, IResult};

// pub fn let_keyword(input: &str) -> IResult<&str, &str> {
//     tag("let")(input)
// }
